/**
 * Путь страницы с реквизитами и QR СБП на фронтенде. Ручной перевод —
 * постоянный способ оплаты, а не заглушка: часть жертвователей принципиально
 * не платит картой онлайн (так же устроен референс).
 *
 * Значение должно совпадать с маршрутом Next.js; маршрут появится на дне 10.
 */
export const MANUAL_TRANSFER_PATH = '/donate/transfer';

/** Страница «спасибо»: опрашивает статус заказа 3 с × 10, потому что вебхук опаздывает. */
export const THANKS_PATH = '/spasibo';

/** Параметр запроса с публичным id заказа — по нему идёт поллинг статуса. */
export const ORDER_QUERY_PARAM = 'order_id';
